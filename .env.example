# Example environment configuration for home-lab-infra-monitor
# Copy this file to ".env" and adjust the values for your environment.
#
# IMPORTANT:
# - Never commit your real ".env" file.
# - Keep secrets (tokens, passwords) out of version control.
# - This example uses Option A: Per-Device Environment Variables
#   See docs/20251119_secrets_handling_options.md for details

# =============================================================================
# Home Assistant
# =============================================================================

# Base URL of your Home Assistant instance
# Example: http://homeassistant.local:8123 or http://192.168.1.100:8123
HA_BASE_URL=http://homeassistant.local:8123

# Optional: long-lived access token for Home Assistant
# Generate in Home Assistant UI: Profile → Security → Long-Lived Access Tokens
# HA_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...


# =============================================================================
# Fritz!Box Monitoring - Option A: Per-Device Credentials (RECOMMENDED)
# =============================================================================
#
# Configure each Fritz!Box with individual credentials for better security:
# - Isolated credentials per device (breach of one doesn't affect others)
# - Different permission levels possible (admin vs. monitoring-only)
# - Easier credential rotation (change one device without affecting others)
#
# Configure up to 10 devices (FRITZBOX_1_* through FRITZBOX_10_*)
# Only configured devices will be monitored (gaps in numbering are OK)

# --- Fritzbox 1 (Example: Main Router / Basement) ---
FRITZBOX_1_HOST=192.168.10.1
FRITZBOX_1_USERNAME=admin
FRITZBOX_1_PASSWORD=changeme_strong_password_1
# Optional: Friendly name for logging and UI display
# FRITZBOX_1_NAME=Main_Router_Basement

# --- Fritzbox 2 (Example: Mesh Repeater / Upper Floor) ---
FRITZBOX_2_HOST=192.168.20.1
FRITZBOX_2_USERNAME=admin
FRITZBOX_2_PASSWORD=changeme_strong_password_2
# FRITZBOX_2_NAME=Mesh_Repeater_UpperFloor

# --- Fritzbox 3 (Example: Guest Network) ---
FRITZBOX_3_HOST=192.168.30.1
FRITZBOX_3_USERNAME=monitoring_user
FRITZBOX_3_PASSWORD=changeme_strong_password_3
# FRITZBOX_3_NAME=Guest_Network

# --- Additional Fritzboxes (optional) ---
# Uncomment and configure as needed:

# FRITZBOX_4_HOST=192.168.40.1
# FRITZBOX_4_USERNAME=admin
# FRITZBOX_4_PASSWORD=changeme_strong_password_4
# FRITZBOX_4_NAME=Office_Network

# FRITZBOX_5_HOST=192.168.50.1
# FRITZBOX_5_USERNAME=admin
# FRITZBOX_5_PASSWORD=changeme_strong_password_5
# FRITZBOX_5_NAME=IoT_Network


# =============================================================================
# Fritz!Box - Legacy Format (DEPRECATED, kept for backward compatibility)
# =============================================================================
#
# ⚠️  DEPRECATED: This format uses shared credentials for all devices
# ⚠️  Migration recommended to per-device format above (Option A)
#
# If you still use this format, it will continue to work but:
# - All devices share the same username/password (security risk!)
# - No per-device credential isolation
# - Harder to rotate credentials
#
# Uncomment only if you need backward compatibility:

# FRITZBOX_HOSTS=192.168.10.1,192.168.20.1,192.168.30.1
# FRITZBOX_USERNAME=shared_admin
# FRITZBOX_PASSWORD=shared_password_for_all_devices


# =============================================================================
# NVMe / Storage Monitoring
# =============================================================================

# Optional: comma-separated list of NVMe device paths to monitor
# If unset or empty, the service may fall back to psutil defaults
#
# Examples:
#   Single device:  NVME_DEVICES=/dev/nvme0n1
#   Multiple:       NVME_DEVICES=/dev/nvme0n1,/dev/nvme1n1,/dev/nvme2n1
#
# List available NVMe devices with: ls -l /dev/nvme*

NVME_DEVICES=/dev/nvme0n1


# =============================================================================
# Advanced Configuration (optional)
# =============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# API server host and port (default: 0.0.0.0:8000)
# API_HOST=0.0.0.0
# API_PORT=8000

# Enable/disable specific monitoring modules
# ENABLE_FRITZBOX_MONITORING=true
# ENABLE_HA_MONITORING=true
# ENABLE_NVME_MONITORING=true


# =============================================================================
# Security Best Practices
# =============================================================================
#
# 1. Use strong, unique passwords for each Fritz!Box (min. 16 characters)
# 2. Create dedicated monitoring users on Fritz!Box with minimal permissions
# 3. Set restrictive file permissions: chmod 600 .env
# 4. Never commit .env to version control (already in .gitignore)
# 5. Store backup of credentials in encrypted password manager
# 6. Rotate credentials every 90 days or after suspected breach
# 7. Monitor Fritz!Box logs for unexpected login attempts
#
# For production deployments, consider migrating to Docker Secrets (Option C)
# See: docs/20251119_secrets_handling_options.md
